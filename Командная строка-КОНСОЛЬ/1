//Консоль
   / Внутренние команды

cd/?
CD Desktop  # параметр в виде пути до нужной папки
cd C:\Users\user\Desktop # Абсолютный путь папки от диска(корня)
cd Desktop\folder # Относительной путь(начиная с места, где мы находимся)
cd .../Downloads
cd .. # Чтобы переместиться на один каталог вверх


cd D:\Photos  # Переход на другой диск
D: # Переход в папку командой ранее
cls # Очистка экрана от всех использованных ранее команд

// Все остальные команды(каждая имеет ключи, способные изменять)
dir # выводит файлы и папки вместе с информацией о дате их создания 
dir/b # хотим видеть только название файлов
copy # лишь файлы
xcopy imgs folder /S/E/Y # сору папку со всем её содержимым | /S/E - ключи

// Внешние команды
notepad # Исполняемый файл блокнота Windows 
set # watch пути, по к-м выполняется командная строка
echo %USERNAME% # get значение переменной
set var=test # get new or old переменные с set + f(x)=value
set /a n=5 # при том if plannin' число for дальнейших вычислений /a - ключ
   # created переменные will exist лишь в текущей сессии, после закрытия консоли nothing saved
setx TEST_VAR 10 # for creation переменной constantly + переменная и value
echo %PATH% # в переменной path хранятс\myProgram\p.exe я наборы каталогов, в к-х интерпритатор строки находит исполняемые файлы
set PATH=%PATH%, C:\Program Files (×86)\myProgram\p.exe # как в случае с блокнотом, добавив путь до программы, по назв-ю исполняемого файла, а не всему пути
p.exe
ipconfig > info.txt # инфу надо copy в файл или save в буфер обмена с информацией сетевых адаптеров и переводом в текст.файл
whoami >> info.txt # позволит не перезаписывать содержимое файла every time
info '<tag'> # чтобы знаки не трактовались как команды, спец знак
clip # copy info сразу в буфер обмена
ipconfig clip # чтобы result 1 команды был передан др
type text.txt # вывести содержимое текст.файла в консоль
type text.txt | clip
clip <text.txt>
chcp 65001 # поменять кодировку консоли с OEM-866 на UTF 8, с номером кодовой страницы 
find string.txt 'Манчестер' # Вывести не всю инфу, а лишь строку *в связке со сторонними файлами
tasklist # в связке с результатом др команд, список всех запущенных процессов в present (окна в браузере, откр страницы

// 
PS> cd C:\Users\realpython\Desktop 
PS> pwd 
C:\Users\realpython\Desktop 
PS> mkdir rp_terminal  # Создание новой папки

PS> cd rp_terminal # Затем перейдите в rp_terminal/ и создайте новый файл с именем hello_terminal.py.
PS> ni hello_terminal.py

touch hello_terminal.py # Linux & MacOs (touch), Windows (ni)

ls # чтобы убедиться, что вы успешно создали файл
echo # для добавления содержания в файл
echo 'print("Hello, terminal!")' > hello_terminal.py # Stdout – место назначения по умолчанию для данных, отправляемых программой командной строки. Данные отображаются на экране, но вы можете указать терминалу перенаправить stdout в файл
> # для отправки вывода в файл
    # Будьте осторожны при перенаправлении stdout на существующие файлы. Любое содержимое файла будет перезаписано без предупреждения.
    # Когда вы перенаправляете вывод команды echo в несуществующий файл, вы создаете файл автоматически 
cat # concatenate

cat hello_terminal.py 
print("Hello, terminal!")

// Управление пакетами с помощью pip
   / Создайте виртуальную среду
venv # Создайте виртуальную среду

PS> python -m venv venv # выполнить следующие шаги, чтобы создать и активировать виртуальную среду
PS> venv\Scripts\activate 
(venv) PS>
deactivate # деактивировать виртуальную среду

// Устанавливаем пакет
В этом разделе вы установите пакет Rich library 
python -m pip list # Чтобы проверить все установленные на данный момент пакеты, вы можете запустить

tasklist | find "notepad" # в скобках название процесса
taskkill /PID 7232  # цифры кода в console, удаление текущего процесса
md new_folder & copy file.txt new_folder\file.txt # создание пустой папки, и копируем в неё какой-то файл

//

batch-script.bat

@echo off
chcp 65001
set error=Ошибка
set success=Успех
set /p input=Введите текст

if %ERRORLEVEL% == 1 (
   echo %error%
) else (
   echo %success%
)

pause
